/*c++ program to print numbrs within the range*/
#include<iostream>
using namespace std;
int main()
{
	int a,b,num;
	cout<<"Enter the range\n";
	cin>>a>>b;
	for(num=a;num<=b;num++)
	{
			if(num==2||num==3||num==5||num==7||num==11)
		{
			cout<<num;
		}
		else if(num%2==0||num%3==0||num%5==0||num%7==0||num%9==0)
		{
			cout<<"\n";
		}
		else
		{
			cout<<num;
		}
	}

}
